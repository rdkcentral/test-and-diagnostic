diff --git a/source/TandDSsp/current_time.c b/source/TandDSsp/current_time.c
index f4dae93..732dead 100644
--- a/source/TandDSsp/current_time.c
+++ b/source/TandDSsp/current_time.c
@@ -209,23 +209,24 @@ void setClockEventFile()
 void activateSystimeTarget() 
 {
     // Check if file already exists
-    if (access(SYSTIME_SET_PATH, F_OK) != -1) 
+    int fileDescriptor = open(SYSTIME_SET_PATH,
+                          O_CREAT | O_EXCL | O_WRONLY,
+                          S_IRUSR | S_IWUSR | S_IRGRP | S_IROTH);
+
+    if (fileDescriptor == -1)
     {
-       CcspTraceInfo(("File system time set file already exists\n"));
+        if (errno == EEXIST)
+        {
+            CcspTraceInfo(("File system time set file already exists\n"));
+        } else
+        {
+            CcspTraceError(("Error creating systime set file: %s\n", strerror(errno)));
+        }
     }
     else
     {
-       // Create the file
-       int fileDescriptor = creat(SYSTIME_SET_PATH, S_IRUSR | S_IWUSR | S_IRGRP | S_IROTH);
-       if (fileDescriptor != -1) 
-       {
-                       close(fileDescriptor);
-               CcspTraceInfo(("File system  time set file created successfully\n"));
-       } 
-       else 
-       {
-               CcspTraceError(("Failed to create system time set file\n"));
-       }
+        close(fileDescriptor);
+        CcspTraceInfo(("File system time set file created successfully\n"));
     }
 }
 bool updateStoredTime(long long new_time) {
diff --git a/source/dmltad/cosa_rdktest_dml.c b/source/dmltad/cosa_rdktest_dml.c
index 118ddd1..286c323 100644
--- a/source/dmltad/cosa_rdktest_dml.c
+++ b/source/dmltad/cosa_rdktest_dml.c
@@ -185,6 +185,7 @@ X_RDK_AutomationTest_GetParamStringValue
         return 0;
     }
     /* AnscTraceWarning(("Unsupported parameter '%s'\n", ParamName)); */
+    dlclose(handle);
     return -1;
  }
 
diff --git a/source/dmltad/cosa_wanconnectivity_operations.c b/source/dmltad/cosa_wanconnectivity_operations.c
index 782b215..878ecf2 100644
--- a/source/dmltad/cosa_wanconnectivity_operations.c
+++ b/source/dmltad/cosa_wanconnectivity_operations.c
@@ -1709,6 +1709,10 @@ int send_query (PCOSA_DML_WANCNCTVTY_CHK_QUERYNOW_CTXT_INFO pQuerynowCtxt, struc
             }
 #endif
     
+            if (pkt_len > sizeof(packet)) {
+                pkt_len = sizeof(packet);   // clamp, avoids Coverity overrun
+            }
+
             // Write packet to socket
             if (write(pfd[idx].fd, packet, pkt_len) < 0) {
                 // Error writing to socket
